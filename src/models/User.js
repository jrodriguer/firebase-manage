/**
 * User class.
 *
 * @constructor
 * @param {String} email - title message to send.
 * @param {String} password - body message to send.
 */
function User(email, password) {
  this.email = email
  this.password = password
}
